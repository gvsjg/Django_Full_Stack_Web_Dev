# Ensure shell scripts always use LF
* text=auto
*.sh text eol=lf

# Ensure Python/YAML/HTML files use LF too
*.py text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.html text eol=lf

# Windows-specific files (CRLF ok here)
*.bat text eol=crlf